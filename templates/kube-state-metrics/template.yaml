repo: https://prometheus-community.github.io/helm-charts
chart: kube-state-metrics
icon: kube-state-metrics-icon.svg

values:
  image:
    registry: olcne
    repository: kube-state-metrics

images:
  - name: .image | (.registry) + "/" + (.repository)
    tag: .image.tag
